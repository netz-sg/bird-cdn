# ========================================
# CDN Configuration
# ========================================
# WICHTIG: Diese Werte bestimmen die generierten URLs!
# Development: CDN_DOMAIN=localhost, CDN_PROTOCOL=http
# Production:  CDN_DOMAIN=cdn.yourdomain.com, CDN_PROTOCOL=https
CDN_DOMAIN=localhost
CDN_PROTOCOL=http
CDN_CACHE_SIZE=50g
CDN_CACHE_INACTIVE=30d

# ========================================
# Let's Encrypt SSL (für Production)
# ========================================
# Email für Let's Encrypt Zertifikat-Benachrichtigungen
LETSENCRYPT_EMAIL=your-email@example.com

# Staging-Modus (true = Test-Zertifikat, false = Production-Zertifikat)
# Empfohlen: Erst mit true testen, dann auf false setzen
LETSENCRYPT_STAGING=true

# MinIO (Origin Storage)
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=adminpassword123
MINIO_ENDPOINT=origin-storage:9000

# Database
DATABASE_URL=postgresql://cdn:cdn123@postgres:5432/cdn

# Redis
REDIS_URL=redis://redis:6379

# Bacdkend API
API_SECRET_KEY=change-this-in-production-very-secret-key-12345
JWT_SECRET=change-this-jwt-secret-key-for-production-67890

# Grafana
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=admin
